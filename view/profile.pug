extend base
block header
 include nav
block  content
    h2 User Page
      .user-container
          p Name :#{user.name}
          p Email:#{user.email}
         
            a(herf="/") Settings
          
          if user.role=="admin"
            p 
              a(href="/managePlans" ) Manage Plans
            p 
              a(href="/manageUser") Manage User
            
              